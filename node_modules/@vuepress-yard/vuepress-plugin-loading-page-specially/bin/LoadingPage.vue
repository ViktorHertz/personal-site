<template>
  <div id="loader-wrapper">
    <dl id="loading-tpl">
      <div id="loading-contain">
        <div class="loading-wrap" id="loading-wrap1">
          <div class="loading-ball" id="loading-ball1"></div>
        </div>

        <div class="loading-wrap" id="loading-wrap2">
          <div class="loading-ball" id="loading-ball2"></div>
        </div>

        <div class="loading-wrap" id="loading-wrap3">
          <div class="loading-ball" id="loading-ball3"></div>
        </div>

        <div class="loading-wrap" id="loading-wrap4">
          <div class="loading-ball" id="loading-ball4"></div>
        </div>
        <div class="loading-wrap" id="loading-wrap5">
          <div class="loading-ball" id="loading-ball5"></div>
        </div>
      </div>
    </dl>
    <h3 class="title" v-if="$frontmatter.home">{{$site.title || $localeConfig.title}}</h3>
    <p
      class="description"
      v-if="$frontmatter.home"
    >{{$site.description || $localeConfig.description}}</p>
  </div>
</template>

<style lang="stylus" scoped>
#loader-wrapper {
  height: 100vh;
  width: 100vw;
  background: #fff;
  background: var(--background-color);

  .title {
    margin: 8rem auto 2rem;
    text-align: center;
    color: #2c3e50;
    color: var(--text-color);
    font-size: 30px;
    box-sizing: border-box;
    padding: 0 10px;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
  }

  .description {
    margin: auto;
    text-align: center;
    color: #2c3e50;
    color: var(--text-color);
    font-size: 22px;
    box-sizing: border-box;
    padding: 0 10px;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
  }

  #loading-tpl {
    position: fixed;
    top: 46%;
    left: 0;
    margin: 0;
    width: 100%;
    text-align: center;
    color: #343434;
    font-size: 14px;
    line-height: 22px;
  }

  #loading-tpl dd {
    margin: 0;
  }

  #loading-contain {
    width: 200px;
    height: 50px;
    position: absolute;
    top: calc(50% + 50px);
    left: calc(50% + 50px);
    opacity: 0;
    animation: fadeIn 1s 1;
    animation-fill-mode: forwards;
  }

  .loading-wrap {
    animation: translateX 1000ms infinite ease-in-out alternate;
    position: absolute;
  }

  .loading-ball {
    width: 50px;
    height: 50px;
    box-shadow: -6.25px -6.25px 0 rgba(0, 0, 0, 0.1) inset;
    background-color: #397BF9;
    border-radius: 50%;
    animation: translateY 5000ms infinite ease-in-out alternate;
  }

  .loading-wrap:after {
    content: '';
    width: 50px;
    height: 7.5px;
    background: #eee;
    position: absolute;
    bottom: 0;
    top: 70px;
    left: 0;
    border-radius: 50%;
    animation: scale 500ms infinite ease-in-out alternate;
  }

  #loading-wrap2, #loading-ball2, #loading-wrap2:after {
    animation-delay: -400ms;
  }

  #loading-wrap3, #loading-ball3, #loading-wrap3:after {
    animation-delay: -800ms;
  }

  #loading-wrap4, #loading-ball4, #loading-wrap4:after {
    animation-delay: -1200ms;
  }

  #loading-wrap5, #loading-ball5, #loading-wrap5:after {
    animation-delay: -1600ms;
  }

  #loading-ball2 {
    background-color: #F4B400;
  }

  #loading-ball3 {
    background-color: #EEEEEE;
  }

  #loading-ball4 {
    background-color: #00A656;
  }

  #loading-ball5 {
    background-color: #E3746B;
  }

  @keyframes translateX {
    100% {
      transform: translateX(-150px);
    }
  }

  @keyframes translateY {
    100% {
      transform: translateY(-100px);
    }
  }

  @keyframes scale {
    100% {
      transform: scale(0.85);
    }
  }

  @keyframes fadeIn {
    100% {
      opacity: 1;
    }
  }
}
</style>
